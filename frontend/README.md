# frontend/

Placeholder. The React + TypeScript + Vite + Tailwind application is created
starting **Phase 3** of [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md), once
there is a backend API worth building a UI against.

Nothing is installed or generated here yet — this file exists only so the
directory is tracked by Git before real code arrives.
