# Direct runtime dependencies, pinned to the exact versions this project
# was built and tested against. Not a full transitive lockfile (pip-tools/uv
# would be overkill for a project this size) - just enough for reproducible
# `pip install -r requirements.txt` runs.
fastapi==0.141.1
uvicorn[standard]==0.52.1
sqlalchemy==2.0.51
alembic==1.19.1
psycopg[binary]==3.3.4
pydantic==2.13.4
pydantic-settings==2.15.0

# Ingestion (Phase 2)
pandas==3.0.5
openpyxl==3.1.5
python-multipart==0.0.32

# Auth / RBAC (Phase 4)
pyjwt==2.13.0
argon2-cffi==25.1.0
email-validator==2.3.0

# Classical ML (Phase 5). No XGBoost/SHAP - HistGradientBoosting and
# permutation_importance already ship in scikit-learn, which avoids two
# additional heavyweight/C-extension dependencies. See app/ml/ docstrings.
scikit-learn==1.9.0

# Enterprise RAG (Phase 7). pgvector: SQLAlchemy Vector column type + numpy
# conversion only (no separate vector DB - see app/rag/__init__.py). pypdf/
# python-docx: pure-Python(ish) text extraction, no heavyweight NLP/CV stack.
# httpx: promoted from dev-only (tests) to a real runtime dependency - the
# optional Ollama/OpenAI-compatible LLM providers make real HTTP calls.
# No sentence-transformers/torch here on purpose - see app/rag/embeddings.py
# for the default zero-download embedding provider and how to opt in to one.
pgvector==0.5.0
pypdf==6.15.0
python-docx==1.2.0
httpx==0.28.1
psycopg2-binary
