# backend/

Placeholder. The FastAPI application (Pydantic schemas, SQLAlchemy models,
API routers, services) is created in **Phase 1** of [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md).

Nothing is installed or generated here yet — this file exists only so the
directory is tracked by Git before real code arrives.
