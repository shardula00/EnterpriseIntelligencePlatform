# infra/

Placeholder. Local infrastructure-as-code lives here starting **Phase 1**:

- `docker-compose.yml` — Postgres (with pgvector), and later MLflow, backend,
  frontend containers.
- Environment templates (`.env.example`) for local service configuration.

Nothing is defined yet — no containers are created in Phase 0.
